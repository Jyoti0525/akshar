"""Reports — AKSHAR.md sections 13, 13b, M8.

`model.py` decides what the report says; `render.py` and `docx_writer.py` lay it
out. One model, three renderings, so the screen, the PDF and the editable file
cannot disagree with each other.
"""
